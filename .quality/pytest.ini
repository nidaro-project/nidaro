[pytest]
# The runner executes pytest from a per-commit project environment that
# contains dependencies + the pinned pytest, never the project code
# itself; pythonpath makes the source importable (v5.1 §4.4).
pythonpath = src .
testpaths = tests
addopts = -q
